type: "runtime"

use_mariadb: true

entrypoint: caddy start --config /etc/caddy/Caddyfile --adapter caddyfile && /Q-n-A

http_proxy: 80

cname:
  - dev-q-n-a.trap.games

https: "hard"

branch:
  main: null

__image: ghcr.io/q-n-a-dev-team/q-n-a_deploy:master
